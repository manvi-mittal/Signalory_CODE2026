# The domain package holds business logic that has nothing to do with
# Flask, HTML, or HTTP. Every function here should be testable by just
# calling it with plain Python values - no app context needed where possible.
